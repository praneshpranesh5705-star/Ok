const timerEl=document.getElementById('timer');const startBtn=document.getElementById('startBtn');const pauseBtn=document.getElementById('pauseBtn');const resetBtn=document.getElementById('resetBtn');const sessionsEl=document.getElementById('sessions');const minutesDoneEl=document.getElementById('minutesDone');const modes=[...document.querySelectorAll('.mode')];let totalSeconds=1500;let remaining=1500;let interval=null;let sessions=Number(localStorage.getItem('focusSessions')||0);let minutesDone=Number(localStorage.getItem('focusMinutes')||0);sessionsEl.textContent=sessions;minutesDoneEl.textContent=minutesDone;function render(){const m=String(Math.floor(remaining/60)).padStart(2,'0');const s=String(remaining%60).padStart(2,'0');timerEl.textContent=`${m}:${s}`;document.title=`${m}:${s} • Focus Timer`}function stop(){clearInterval(interval);interval=null}function start(){if(interval)return;interval=setInterval(()=>{remaining--;render();if(remaining<=0){stop();sessions++;minutesDone+=Math.round(totalSeconds/60);localStorage.setItem('focusSessions',sessions);localStorage.setItem('focusMinutes',minutesDone);sessionsEl.textContent=sessions;minutesDoneEl.textContent=minutesDone;alert('Time is up! Great work.');}},1000)}function reset(){stop();remaining=totalSeconds;render()}startBtn.addEventListener('click',start);pauseBtn.addEventListener('click',stop);resetBtn.addEventListener('click',reset);modes.forEach(btn=>btn.addEventListener('click',()=>{modes.forEach(x=>x.classList.remove('active'));btn.classList.add('active');totalSeconds=Number(btn.dataset.minutes)*60;remaining=totalSeconds;stop();render()}));render();
